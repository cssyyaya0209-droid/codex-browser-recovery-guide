# v2.0 发布验证

日期：2026-09-09。以下检查针对本次可配置版本；不包含重新启动整个桌面应用后的浏览器计时。

| 检查 | 验证范围 |
| --- | --- |
| 构建 | Windows x64 C# 包装程序编译成功 |
| 配置校验 | 缺少文件、空值、未替换模板、错误协议、非本地地址、凭据、路径、查询参数、无效端口、多行内容，共 11 类输入，启动器与程序均拒绝 |
| 单份配置 | 同一个 EXE 在两组不同临时端口下读取同一 proxy.txt；仅修改文件即可切换 |
| 启动预检查 | 两组配置下均通过，使用明确的本机 AppPath 与 NodePath |
| 实际 MCP | 在原本缺少代理变量的测试父环境下，原版 node_repl 完成 initialize；检查其实际 HTTP/HTTPS 变量与配置一致 |
| 生命周期 | 关闭输入后，测试包装程序与原版子进程均正常退出，没有留下测试子进程 |

端口测试使用临时 TCP 监听器验证配置和端口选择，不代表该监听器提供 HTTP 代理，也不代表目标网站网络已验证。

自动安装包查询在受限测试账户下没有找到桌面用户的安装包，脚本按设计提示使用 -AppPath；未把该项标为自动识别通过。部署时应在实际桌面用户的 PowerShell 7 中操作，必要时使用明确路径。

真实桌面重启后消除固定延迟的历史记录来自此前固定参数版本，见 [技术说明](TECHNICAL-NOTES.md)。v2.0 已验证配置传递和实际 MCP 行为，但每台电脑仍需完成 README 中的浏览器验收。本次没有中断正在工作的 Codex。
